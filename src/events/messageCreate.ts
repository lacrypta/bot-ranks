import { Message } from 'discord.js';
import { BotEvent } from '../types/botEvents';

const event: BotEvent = {
  //   name: 'messageCreate',
  //   once: false,
  //   execute: async (message: Message) => {
  //     console.log(`[messageCreate.ts] ${message.author.tag} said: ${message}`);
  //   },
};

export default event;
